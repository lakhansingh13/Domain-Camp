#include<bits/stdc++.h>
using namespace std;

int sum(int n){
    if(n>0){
        return(n+ sum(n-1));
    }
    return 0;
}

int main(){
    
    int n;
    cout<<"Enter the value of n: ";
    cin>>n;
    cout<<"The sum of "<<n<<" natural number is: "<<sum(n);
}
